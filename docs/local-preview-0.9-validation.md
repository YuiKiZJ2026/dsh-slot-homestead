# 0.9 本地评审版验证记录

本记录只证明已执行的检查，不代表已发布或已通过正式桌面环境验收。

## 已验证

- 独立目录、无既有依赖，使用 Node 24.19.0 / npm 11.9.0 执行 `npm ci`、类型检查、构建与打包。
- 645 项单元/组件测试，另有 7 项安装包契约检查。
- 覆盖率：语句 88.07%，分支 81.02%，函数 86.30%，行 90.69%。
- 23 项浏览器流程检查：3 项新玩法、11 项原生预览与交互、9 项应用流程和昼夜光照。
- 新玩法覆盖：每日委托不能重复领奖、跨日与时钟回退、12 周每日奖励有界、供应上限、旧存档、Host 命令重放与重启、并发播种、下一茬保留当前产物，以及收获金币到账。
- UI 覆盖：560×384 / 展开后 560×496 原生尺寸、手账开关与焦点返回、工坊搜索、搜索不改变已获得的套装进度、三处生态切换、拉杆与出奖动画。
- 构建产物：`dsh-slot-homestead-0.9.0-preview.1.tgz`，仅保存在本机。

确切安装包 SHA-256：`388d9cd60349d73233be5fa3169af134e9b4df622a8027c959d32ce34164276a`。

`.npmrc` 明确沿用原有 CI 的 `legacy-peer-deps` 策略：DSH 提供宿主依赖，独立开发预览使用自己的 React 版本。该策略不能替代真实宿主安装验证。

## 尚未完成

- 真实 DSH 的新包安装/重启持久化验收：隔离环境的 DSH CLI 依赖安装长时间无进度，已停止；没有替换用户的正式桌面插件。
- 美术人工验收与新版截图基线：保留原有桌子、老虎机结构和月亮，增量调整像素材质与控制界面；没有更新旧截图基线来制造“通过”。
- GitHub / DSH 市场上传、发布：未执行，须维护者另行确认。

正式发布前应使用最终确切 tgz 执行 `scripts/dsh-real-smoke.mjs`（含新增手账与播种的二次重启检查），并在真实桌面窗口人工确认交互与存档；预览沙盒的刷新重置不能视为持久化验证。
